#version 330 core

in vec3 fColor;
in vec2 uv;

uniform sampler2D texture_dds;
// Ouput data
out vec3 color;

void main()
{

	// Output color = red 
	color = fColor * texture(texture_dds, uv).rgb;
}